
const PrimaryBtn = ({btnText}) => {
    return (
        <div>
           <button className="btn bg-primary-color border-primary-color text-white">{btnText}</button> 
        </div>
    );
};

export default PrimaryBtn;